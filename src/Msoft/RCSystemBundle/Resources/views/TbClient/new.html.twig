{% extends '::base.html.twig' %}

{% block content %}
    <h1>{{titleAction}}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tb_client') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
