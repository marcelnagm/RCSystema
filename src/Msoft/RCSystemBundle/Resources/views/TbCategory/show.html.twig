{% extends '::base.html.twig' %}

{% block content %}
    <h1>TbCategory</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tb_categorty') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tb_categorty_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
